# Server (Express)
cd server
npm install
npm run dev

The development server uses temporary in-memory storage, so no MongoDB connection is required.
Donors and blood requests reset whenever the server restarts.
